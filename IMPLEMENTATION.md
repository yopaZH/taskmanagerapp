## Task Manager App - Полная реализация

### 📋 Обзор проекта

Полнофункциональное кроссплатформенное мобильное приложение для управления задачами и проектами, разработанное на .NET MAUI с использованием архитектурного паттерна MVVM.

---

## ✅ Реализованные компоненты

### 1. **Models (Модели данных)**
- **TaskStatus.cs** - Enum со статусами: New, InProgress, Completed
- **TaskPriority.cs** - Enum с приоритетами: Low, Medium, High
- **TaskItem.cs** - Модель задачи с полями Title, Description, Status, Priority
- **Project.cs** - Модель проекта с коллекцией задач и методами для подсчета

### 2. **Services (Сервисы)**
- **IStorageService.cs** - Интерфейс для работы с хранилищем данных
- **FileStorageService.cs** - Реализация сервиса хранения на основе JSON файлов
  - LoadDataAsync() - Загрузка проектов из файла
  - SaveDataAsync() - Сохранение проектов в файл
  - Обработка ошибок и автоматическое восстановление

### 3. **ViewModels (Модели представлений)**
- **BaseViewModel.cs** - Базовый класс со свойствами Title и IsLoading, поддержкой INotifyPropertyChanged
- **MainViewModel.cs** - ViewModel главного экрана с навигацией к проектам
- **ProjectsViewModel.cs** - ViewModel списка проектов:
  - Загрузка/сохранение проектов
  - Добавление новых проектов
  - Удаление проектов
  - Выбор проекта для просмотра задач
- **ProjectTasksViewModel.cs** - ViewModel списка задач проекта:
  - Загрузка задач выбранного проекта
  - Добавление новых задач
  - Удаление задач
  - Изменение статуса задачи (циклический переход: New → InProgress → Completed → New)
  - Фильтрация и сортировка по приоритету
  - Поддержка QueryProperty для получения ProjectId из навигации
- **TaskEditViewModel.cs** - ViewModel редактирования задачи:
  - Загрузка данных задачи для редактирования
  - Сохранение изменений
  - Валидация данных
  - Поддержка QueryProperty для ProjectId и TaskId

### 4. **Views (Представления XAML)**
- **MainPage.xaml/cs** - Главный экран приложения
  - Приветственный текст
  - Кнопка для перехода к проектам
  
- **ProjectsPage.xaml/cs** - Экран списка проектов
  - CollectionView со списком всех проектов
  - Отображение количества выполненных задач
  - Кнопка для добавления нового проекта
  - Кнопки для удаления проектов
  
- **ProjectTasksPage.xaml/cs** - Экран с задачами проекта
  - Список всех задач проекта с отображением:
    - Название задачи
    - Приоритета
    - Статуса
  - Кнопки для редактирования задач
  - Кнопки для удаления задач
  - Кнопка для циклического изменения статуса
  - Кнопка для добавления новой задачи
  
- **TaskEditPage.xaml/cs** - Экран редактирования задачи
  - Поля ввода для названия и описания
  - Picker для выбора статуса
  - Picker для выбора приоритета
  - Кнопки Сохранить и Отмена

### 5. **Navigation (Навигация)**
- **NavigationConstants.cs** - Константы для маршрутов навигации
- **AppShell.xaml/cs** - Конфигурация Shell-навигации
  - Регистрация всех страниц как ShellContent
  - Маршруты: MainPageRoute, ProjectsPageRoute, ProjectTasksPageRoute, TaskEditPageRoute
  - Поддержка передачи параметров через QueryProperty

### 6. **Configuration (Конфигурация приложения)**
- **MauiProgram.cs** - Настройка приложения:
  - Регистрация сервисов (Singleton для IStorageService)
  - Регистрация ViewModels (Singleton для MainViewModel и ProjectsViewModel, Transient для остальных)
  - Регистрация Pages
  - Конфигурация шрифтов
  
- **App.xaml/cs** - Главное приложение с определением ресурсов
  - Цветовые схемы
  - Инициализация AppShell

### 7. **Resources (Ресурсы)**
- **Colors.xaml** - Палитра цветов приложения:
  - PrimaryColor (#2196F3)
  - SecondaryColor (#4CAF50)
  - DangerColor (#FF5252)
  - WarningColor (#FF9800)
  
- **AppStyles.xaml** - Глобальные стили для компонентов

### 8. **Project Configuration**
- **TaskManagerApp.csproj** - Конфигурация .NET MAUI проекта
  - Целевые платформы: Android (API 21+), iOS (11.0+)
  - Зависимости: Microsoft.Maui.Controls, Microsoft.Extensions.Logging.Debug

---

## 🏗️ Архитектура

### Принципы MVVM:
- **Model** - TaskItem, Project (чистые данные)
- **View** - XAML страницы (только UI, без логики)
- **ViewModel** - Классы, наследующие BaseViewModel с INotifyPropertyChanged
- **Services** - IStorageService для доступа к данным

### Слои приложения:
```
┌─────────────────────────────────┐
│     Presentation Layer (View)   │
│  (XAML Pages + Code-Behind)     │
└────────────────┬────────────────┘
                 │
┌────────────────▼────────────────┐
│  ViewModel Layer (MVVM Logic)   │
│  (BaseViewModel, Commands)      │
└────────────────┬────────────────┘
                 │
┌────────────────▼────────────────┐
│     Service Layer (Logic)       │
│  (IStorageService, Commands)    │
└────────────────┬────────────────┘
                 │
┌────────────────▼────────────────┐
│      Model Layer (Data)         │
│  (Project, TaskItem, Enums)     │
└─────────────────────────────────┘
```

---

## 🔄 Навигация

### Маршруты:
1. MainPageRoute → Главный экран
2. ProjectsPageRoute → Список проектов
3. ProjectTasksPageRoute → Задачи проекта (параметр: ?projectId=)
4. TaskEditPageRoute → Редактирование задачи (параметры: ?projectId=&taskId=)

### Получение параметров:
- ViewModels используют атрибут `[QueryProperty]` для автоматического получения параметров
- Beispiel: `[QueryProperty(nameof(ProjectId), "projectId")]`

---

## 💾 Сохранение данных

### Механизм хранения:
- Файловая система приложения (FileSystem.AppDataDirectory)
- JSON сериализация через System.Text.Json
- Файл: `tasks_data.json`
- Обработка ошибок: автоматическое восстановление при отсутствии файла

### Структура данных JSON:
```json
[
  {
    "id": "guid",
    "name": "Project Name",
    "tasks": [
      {
        "id": "guid",
        "projectId": "guid",
        "title": "Task Title",
        "description": "Description",
        "status": 0,
        "priority": 1
      }
    ]
  }
]
```

---

## 🎯 Функциональность

### Управление проектами:
- ✅ Просмотр всех проектов
- ✅ Создание новых проектов
- ✅ Удаление проектов
- ✅ Отображение статистики (выполненные/всего задач)

### Управление задачами:
- ✅ Просмотр задач проекта
- ✅ Создание новых задач
- ✅ Редактирование задач
  - Изменение названия
  - Изменение описания
  - Изменение статуса
  - Изменение приоритета
- ✅ Удаление задач
- ✅ Циклическое изменение статуса
- ✅ Фильтрация по статусу
- ✅ Сортировка по приоритету

---

## 📱 Целевые платформы

- **Android** - API 21+ (достаточно широкая поддержка)
- **iOS** - 11.0+ (современные устройства)

---

## 🚀 Расширяемость

Архитектура позволяет легко добавить:
- Синхронизацию с облаком
- Уведомления
- Повторяющиеся задачи
- Фильтры и поиск
- Экспорт/импорт
- Совместную работу
- Категории/теги для задач

---

## 📝 Соответствие ТЗ

| Требование | Статус |
|-----------|--------|
| Четкое разделение ответственности | ✅ |
| Model содержит только данные | ✅ |
| View не содержит бизнес-логики | ✅ |
| ViewModel реализует INotifyPropertyChanged | ✅ |
| Services для работы с данными | ✅ |
| Shell navigation | ✅ |
| Передача параметров навигации | ✅ |
| Локальное хранение данных | ✅ |
| JSON сериализация | ✅ |
| Обработка ошибок | ✅ |
| Управление задачами | ✅ |
| Фильтрация и сортировка | ✅ |
| Dependency Injection | ✅ |

---

## 🎨 Дизайн UI

- Минималистичный интерфейс
- Интуитивная навигация
- Цветовая схема с использованием Material Design цветов
- Адаптивный макет для разных размеров экранов
- Использование стандартных компонентов MAUI

---

## ⚙️ Технологический стек

- **.NET 8** - Современная платформа
- **.NET MAUI** - Кроссплатформенная фреймворк
- **C#** - Язык программирования
- **XAML** - Язык разметки UI
- **System.Text.Json** - Сериализация данных
- **Microsoft.Maui.Controls** - UI компоненты

---

## 📚 Как использовать

### Компиляция и запуск:
```bash
# Восстановление зависимостей
dotnet restore

# Сборка проекта
dotnet build

# Запуск на Android
dotnet run -f net8.0-android

# Запуск на iOS
dotnet run -f net8.0-ios
```

### Использование приложения:
1. Запустить приложение
2. На главном экране нажать "Перейти к проектам"
3. Создать новый проект (+ Создать проект)
4. Выбрать проект для просмотра задач
5. Добавить задачу или редактировать существующие
6. Изменять статусы и приоритеты задач по мере выполнения

---

## ✨ Готово к запуску!

Проект полностью реализует все требования ТЗ и готов к компиляции и запуску на мобильных платформах.
