# 📱 Как собрать и установить APK

## Вариант 1: Автоматическая установка через скрипт (рекомендуется)

### Требования
- Linux с `adb` (Android Debug Bridge)
- GitHub CLI (`gh`)
- Android устройство с USB debugging включен

### Шаги

1. **Установите GitHub CLI** (если нет):
   ```bash
   curl -sS https://cli.github.com/install.sh | sudo bash
   ```

2. **Авторизуйтесь в GitHub** (первый раз):
   ```bash
   gh auth login
   ```

3. **Подключите телефон по USB** и включите USB debugging:
   - Settings → Developer Options → USB Debugging (включить)
   - Проверьте подключение:
     ```bash
     adb devices
     ```

4. **Запустите скрипт установки**:
   ```bash
   cd /home/styopa/Documents/uni/рпп/taskmanagerapp
   chmod +x scripts/install_apk_from_actions.sh
   
   # Вариант 1: автоматически (repo из имени пользователя На GitHub)
   ./scripts/install_apk_from_actions.sh
   
   # Вариант 2: указать репозиторий явно
   ./scripts/install_apk_from_actions.sh "" "your-username/taskmanagerapp"
   ```

Скрипт автоматически:
- Скачает последний успешный APK из workflow
- Установит его на подключенное устройство
- Запустит приложение (если нужно)

---

## Вариант 2: Ручная загрузка и установка

### Шаг 1: Запустить workflow вручную
1. Перейдите на GitHub: `https://github.com/your-username/taskmanagerapp`
2. Откройте вкладку **Actions**
3. Выберите workflow **Build Android APK**
4. Нажмите **Run workflow** (или он запустится автоматически при push)

### Шаг 2: Дождитесь завершения
- Скрипт будет выполняться ~5-10 минут
- Статус должен показать ✅ зеленую галочку

### Шаг 3: Скачайте APK
1. Откройте прогон workflow
2. Внизу найдите **Artifacts**
3. Скачайте `android-apk-release`

### Шаг 4: Установите на телефон
```bash
# Убедитесь что устройство подключено
adb devices

# Установите APK (замените path/to/app.apk на путь к вашему файлу)
adb install -r /path/to/android-apk-release/app.apk
```

### Шаг 5: Запустите приложение
```bash
# Опция 1: найти в меню приложений телефона и нажать

# Опция 2: запустить через adb
adb shell am start -n com.taskmanager.app/.MainActivity
```

---

## Вариант 3: Локальная сборка (Windows/macOS)

На Windows или macOS с установленным .NET MAUI SDK:

```bash
cd /home/styopa/Documents/uni/рпп/taskmanagerapp

# Установить workloads
dotnet workload restore
dotnet workload install maui-android

# Собрать APK
dotnet publish -f net8.0-android -c Release \
  -p:AndroidPackageFormat=apk \
  -p:AndroidKeyStore=false

# APK будет в: bin/Release/net8.0-android/
# Или используйте adb для установки
adb install -r bin/Release/net8.0-android/*.apk
```

---

## 🔍 Версии поддержки

- **Android**: API 21+ (Android 5.0 Lollipop и выше)
- **iOS**: 11.0+ (собирается на macOS)

---

## ❓ Часто задаваемые вопросы

### Q: "adb: command not found"
A: Установите Android platform-tools:
```bash
# Ubuntu/Debian
sudo apt install android-tools-adb

# Fedora
sudo dnf install android-tools

# macOS
brew install android-platform-tools
```

### Q: "Device not found"
A: Проверьте:
1. Телефон подключен по USB
2. USB debugging включен в настройках
3. Вы авторизовали компьютер на телефоне
```bash
adb devices  # должно показать ваше устройство
```

### Q: "Workflow not found"
A:
1. Убедитесь что файл `.github/workflows/android-build.yml` в репозитории
2. Запушьте изменения в `main` или `master` ветку
3. Дождитесь запуска workflow в Actions

### Q: "Installation failed: Unknown Error"
A:
1. Попробуйте удалить предыдущую версию:
   ```bash
   adb uninstall com.taskmanager.app
   ```
2. Переподключитесь:
   ```bash
   adb disconnect
   adb connect <device-ip>  # если по сети
   # или переподключитесь по USB
   ```

---

## 📝 Структура артефактов

Workflow создает APK в разных местах в зависимости от конфигурации:
- `bin/Release/net8.0-android/com.taskmanager.app-release.apk`
- `publish/com.taskmanager.app-release.apk`

Скрипт автоматически находит их.

---

## 🚀 Цель и следующие шаги

Устанавливав APK на Android устройство — готово к использованию!

Для продвинутого использования:
- **Подпись APK**: добавить keystore + signing в workflow
- **Публикация в Google Play**: требует подписанного APK и Dev Account
- **Автоматическое тестирование**: добавить UI тесты в workflow
